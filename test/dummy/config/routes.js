/**
 * define routes and order of routes here
 */

var routes = module.exports = function appRoutes( router ){

  //router.use( 'konter' );

  //router.use( 'konter-web' );

  router.use( 'blogs' );

}